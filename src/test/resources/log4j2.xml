<?xml version="1.0" encoding="UTF-8"?>
<Configuration status="info" name="Log4j2 Configuration">
    <Properties>
        <Property name="log.path">./logs</Property>
        <Property name="log.pattern">%d{ISO8601} [%-5p] [%t] %c{1} - %m%n</Property>
    </Properties>

    <Appenders>
        <!-- Console Appender -->
        <Console name="Console" target="SYSTEM_OUT">
            <PatternLayout pattern="${log.pattern}" />
        </Console>

        <!-- File Appender -->
        <File name="File" fileName="${log.path}/qa-api-automation.log">
            <PatternLayout pattern="${log.pattern}" />
        </File>

        <!-- Rolling File Appender -->
        <RollingFile name="RollingFile" fileName="${log.path}/qa-api-automation.log"
                     filePattern="${log.path}/qa-api-automation-%d{yyyy-MM-dd}-%i.log">
            <PatternLayout pattern="${log.pattern}" />
            <Policies>
                <SizeBasedTriggeringPolicy size="10MB" />
                <TimeBasedTriggeringPolicy interval="1" modulate="true" />
            </Policies>
            <DefaultRolloverStrategy max="10" />
        </RollingFile>
    </Appenders>

    <Loggers>
        <!-- Root Logger -->
        <Root level="info">
            <AppenderRef ref="Console" />
            <AppenderRef ref="RollingFile" />
        </Root>

        <!-- Package specific loggers -->
        <Logger name="com.agibank.api" level="info" additivity="false">
            <AppenderRef ref="Console" />
            <AppenderRef ref="RollingFile" />
        </Logger>

        <!-- RestAssured -->
        <Logger name="io.restassured" level="debug">
            <AppenderRef ref="Console" />
            <AppenderRef ref="RollingFile" />
        </Logger>
    </Loggers>
</Configuration>
